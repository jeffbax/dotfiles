if [ -x /opt/homebrew/bin/brew ]; then
  eval "$(/opt/homebrew/bin/brew shellenv bash)"
fi

export BASH_ENV="$HOME/.config/bash/chezmoi/mise.bash"

if [ -r "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc"
fi
